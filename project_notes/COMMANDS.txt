
# * mysql migration with better-auth(auth lib) powershell
$env:MYSQL_HOST="localhost"
$env:MYSQL_PORT="3306"
$env:MYSQL_DATABASE="my_database"
$env:MYSQL_USER="root"
$env:MYSQL_PASSWORD="Fuckyouowl_742"
pnpx @better-auth/cli@latest migrate

or

pnpx @better-auth/cli@latest generate

# * mysql migration with better-auth(auth lib) cmd
             (localhost)
set MYSQL_HOST=localhost
set MYSQL_PORT=3306
set MYSQL_DATABASE=nextjs_playground_local_db
set MYSQL_USER=root
set MYSQL_PASSWORD=Fuckyouowl_742
pnpx @better-auth/cli@latest generate

or 

pnpx @better-auth/cli@latest migrate

             (production)
set MYSQL_HOST=test-mysql-db.c7e0kwssov9h.ap-southeast-2.rds.amazonaws.com
set MYSQL_PORT=3306
set MYSQL_DATABASE=nextjs_playground_prod_db
set MYSQL_USER=admin
set MYSQL_PASSWORD=Fuckyouowl_742
pnpx @better-auth/cli@latest generate

or 

pnpx @better-auth/cli@latest migrate


# * show only hidden files on a folder
dir /ah

# * show the list of remote repo's
git remote -v

# * check list of git branch
git branch

