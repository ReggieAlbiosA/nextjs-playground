# * ⚙ Step 1: Set up Next.js locally on VSCode
# TODO 1️⃣ Install Node.js
Make sure you have Node.js installed (recommend Node 18+).
👉 Check by running:

node -v

# TODO2️⃣ Create Next.js project
In your terminal:

pnpx create-next-app@latest

It will ask:

Project name → nextjs-playground (or whatever you want)

Typescript? → Yes/No (I recommend yes if you want type safety)

ESLint? → Yes (recommended)

Tailwind? → Yes (if you want it preconfigured, optional)

src/ directory? → Yes (optional)

App Router? → Yes (recommended, this is the modern Next.js architecture)

Once it’s done, you’ll have a full Next.js setup.

# TODO3️⃣ Open in VSCode

cd nextjs-playground
code .

# TODO4️⃣ Run locally

pnpm dev
Visit http://localhost:3000 → you should see the default Next.js page.

# * 🚀 Step 2: Push to GitHub
# TODO1️⃣ Initialize git:

git init
git add .
git commit -m "Initial commit"

# TODO 2️⃣ Create a GitHub repository
Go to GitHub → New repository → name it nextjs-playground.

Don’t initialize with a README (you already have local files).

# TODO 3️⃣ Link local project to GitHub & push

git remote add origin https://github.com/yourusername/nextjs-playground.git (choose for simplicity)

or

git remote add origin git@github.com:yourusername/nextjs-playground.git (choose for speed and security)
git branch -M main
git push -u origin main

# * 🌍 Step 3: Deploy on Vercel
# TODO1️⃣ Go to https://vercel.com → sign up or log in.

# TODO2️⃣ Click New Project → Import Git Repository.

# TODO3️⃣ Select your nextjs-playground repo.

# TODO4️⃣ Configure:

Framework preset: Next.js (auto-detected)

Root directory: usually / (unless you put it in a subfolder)

Environment variables: if any (skip for now)

# TODO5️⃣ Click Deploy.